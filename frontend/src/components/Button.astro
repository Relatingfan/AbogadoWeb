---
import { HoverButton } from './ui/hover-glow-button';

interface Props {
  text: string;
  href?: string;
  variant?: 'primary' | 'secondary' | 'outline';
  class?: string;
  type?: 'button' | 'submit' | 'reset';
  id?: string;
}

const { text, href, variant = 'primary', class: className = '', type = 'button', id } = Astro.props;

// Determine colors based on variants following firm branding
const config = {
  primary: {
    backgroundColor: '#0A1930', // --color-primary
    glowColor: '#B8C5D6', // --color-gold glow (now platinum) makes it stand out beautifully
    textColor: '#FFFFFF', // white text on primary
    hoverTextColor: '#B8C5D6', // --color-gold (now platinum)
    customClass: 'border-2 border-transparent shadow-[0_4px_20px_rgba(10,25,48,0.3)] hover:shadow-[0_6px_25px_rgba(10,25,48,0.5)]'
  },
  secondary: {
    backgroundColor: '#0A0D14', // --color-text
    glowColor: '#0A1930',
    textColor: '#FFFFFF', // light text on dark secondary
    hoverTextColor: '#FFFFFF',
    customClass: 'border-2 border-transparent'
  },
  outline: {
    backgroundColor: 'transparent',
    glowColor: '#0A1930',
    textColor: '#0A0D14', // dark text on transparent bg
    hoverTextColor: '#B8C5D6',
    customClass: 'border-2 border-[#0A1930] hover:border-[#B8C5D6] transition-colors duration-300'
  }
};

const currentConfig = config[variant];
---

<HoverButton
  client:visible
  id={id}
  href={href}
  type={type}
  glowColor={currentConfig.glowColor}
  backgroundColor={currentConfig.backgroundColor}
  textColor={currentConfig.textColor}
  hoverTextColor={currentConfig.hoverTextColor}
  className={`${currentConfig.customClass} ${className}`}
>
  {text}
</HoverButton>
